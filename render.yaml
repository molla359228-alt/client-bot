services:
  - type: web
    name: client-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python client_bot.py
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false
